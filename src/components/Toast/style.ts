import { StyleSheet } from "react-native";

export const toastcss = StyleSheet.create({
  container: {
    position: "absolute",
    alignItems: "center",
    width: "100%",
    elevation: 1,
    paddingTop: 100,
  },
});
